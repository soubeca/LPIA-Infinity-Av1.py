# Calculando a base de um retângulo

base = float(input('Informe a base do seu triângulo: '))
altura = float(input('Agora informe a altura do seu triângulo: '))

area = base*altura
print(f'A áre do triângulo é de {area} m².')